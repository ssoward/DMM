package com.soward.controller;

/**
 * Created by ssoward on 9/6/14.
 */
public class POController {
    //create new purchase order:


//    String username = (String) session.getAttribute("username");

//    insert into Invoices (invoiceNum, accountNum, invoiceDate, locationNum, username, invoiceTotal, invoiceTax, invoiceShipTotal, invoicePaid, invoiceReceivedBy, invoiceContactNum, invoiceReferenceNum, invoiceChargeStatus, paymentMethod1)
//    values ($invoiceNum, $accountNum, '$invoiceDate', $locationNum, '$username', 0.00, 0.00, 0.00, 0.00, '$supplierName', 'PO', NULL, NULL,NULL));

}
